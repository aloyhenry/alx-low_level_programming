#include <stdio.h>
#include <stdlib.h>
#include <time.h>
 
/**
 * main - prints numbers 0-9 without using char or printf/puts,
 * and putchar 2x max
 *
 * Return: 0 on success
 */

int main(void)
{
        int i;

        for (i = '0'; d <= '9'; i++)
                putchar(i);
        putchar('\n');

        return (0);
}
